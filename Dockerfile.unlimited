FROM nvidia/cuda:11.8.0-cudnn8-devel-ubuntu22.04
# FIXED: 37步分离安装，避免大包冲突
RUN pip install --timeout=600 --retries=3 transformers>=4.30.0
RUN pip install --timeout=600 --retries=3 accelerate>=0.20.0
RUN pip install --timeout=600 --retries=3 diffusers>=0.18.0
